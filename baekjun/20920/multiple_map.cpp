#include <ios>
#include <iostream>
#include <map>
#include <string>
using namespace std;

int main() {
  ios_base::sync_with_stdio(false);
  cin.tie(nullptr);
  int m, n;
  string buf;
  cin >> n;
  cin >> m;
  for (int i = 0; i < n; i++) {
    cin >> buf;
  }
  return 0;
}
